{
  "name": "fcd-site",
  "version": "0.0.1",
  "description": "",
  "license": "Apache-2.0",
  "scripts": {
    "start": "npm run build-css && npm run budo",
    "build": "browserify src/js/index.js > public/index.js",
    "buildmin": "browserify -g uglifyify src/js/index.js > public/index.js",
    "build-css": "node-sass --include-path scss src/scss/index.scss public/index.css",
    "budo": "budo src/js/index.js:index.js --dir ./public --live --verbose --port 3001",
    "discify": "browserify --full-paths src/js/index.js | discify --open"
  },
  "dependencies": {
    "bezier-easing": "2.0.3",
    "eventemitter3": "2.0.2"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.16.0",
    "babel-preset-stage-2": "^6.22.0",
    "babelify": "^7.3.0",
    "browserify": "^14.0.0",
    "budo": "^9.4.7",
    "derequire": "^2.0.3",
    "discify": "^1.6.0",
    "node-sass": "^4.5.3"
  },
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": [
            "es2015"
          ]
        }
      ]
    ]
  }
}
